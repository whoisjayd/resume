[project]
name = "resume"
version = "0.1.0"
description = "A version-controlled professional resume built with RenderCV"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "rendercv[full]>=2.8",
]
